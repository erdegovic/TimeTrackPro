node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
.env
.env.*
!.env.example
.local/
tmp/

# Local curl/session artefacts — never commit session material
cookies.txt

# Audit hand-off artefacts — review then delete, never commit
_to_delete/
tickd-polish.patch
